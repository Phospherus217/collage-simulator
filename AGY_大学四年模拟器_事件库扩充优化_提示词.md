# AGY 任务：《大学四年模拟器》现有事件库扩充与优化
# ——严格保持现有游戏逻辑，只增加各事件组中的事件数量

你现在需要修改和扩充一个已经可以运行的《大学四年模拟器》项目。

本任务不是重新设计游戏系统，不是重构事件调度器，也不是重新定义路线玩法。

你的工作目标非常明确：

> **完整保留当前游戏已有玩法、分支开放逻辑、月度选择逻辑和体力限制，只在现有事件组内部增加更多高质量大学生活事件，并修复新增事件可能出现的时间冲突、剧情顺序冲突和重复问题。**

请先完整阅读项目文件，再开始修改。

---

# 一、第一原则：现有游戏玩法已经确定，不允许改变

本项目的基本交互逻辑必须完整保留。

核心逻辑是：

```text
进入一个新的月份
↓
读取已经开放的分支
↓
已经开放的分支本月仍然可以选择
↓
查看该分支当前月份可见的事件
↓
事件是否处于开放月份
    ↓
是 → 玩家可以看到
否 → 当前月份不显示
↓
检查玩家当前体力/行动资源
    ↓
足够 → 可以选择该事件
不足 → 可以看到，但不能执行
↓
执行事件
↓
结算原有数值/状态
↓
玩家仍可继续选择其他已开放分支
↓
直到本月资源不足或玩家结束月份
```

这就是当前游戏的基本玩法。

不得改变。

---

# 二、必须冻结的核心游戏逻辑

本轮禁止修改以下机制：

## 1. 月度循环

不得改变：

```text
一个月一次的 Stage / 月度推进方式
```

不得：

- 新增周系统；
- 新增日系统；
- 把一个月拆成多个新的回合结构；
- 改变进入下个月的条件。

## 2. 分支选择方式

已经开放的分支：

> 每个月都应该可以进入。

例如：

```text
学业
社交
恋爱
家庭
健康
工作
考研
保研
AI-Lab
……
```

只要该分支按照现有规则已经开放：

> 不允许因为新增事件系统而让整个分支随机消失。

不得新增：

```text
本月随机只开放3个分支
随机抽取分支
轮换分支
每日分支
```

等机制。

---

# 三、事件的核心显示规则必须保持简单

对于已经开放的分支：

事件是否能够看到，主要取决于：

```text
当前月份
是否落在该事件允许出现的月份范围内
```

例如：

```text
event.open_month = M8
event.close_month = M12
```

那么：

```text
M7
不可见

M8~M12
可见

M13以后
不再出现
```

保持项目当前已有实现。

不得为了新增事件重新设计：

```text
复杂权重池
随机抽卡
隐藏概率
事件热度
事件疲劳
全局事件冷却器
复杂事件导演器
```

除非这些机制本来就已经存在于项目中。

---

# 四、体力决定“能不能选”，时间决定“能不能看到”

这是本任务必须严格保护的交互原则。

需要区分：

## 可见性

主要由：

```text
event 的开放月份
```

控制。

## 可执行性

主要由：

```text
玩家当前体力
TU / EP
或当前项目已经使用的行动资源
```

控制。

因此：

```text
事件已经到了开放时间
+
体力不足
```

正确表现应该是：

> 玩家仍然能看到这个机会，但当前没有足够体力执行。

而不是：

> 直接让事件消失。

必须保留当前游戏已有的具体 UI 表现方式。

---

# 五、本轮真正需要做的工作

这次只做：

> **扩充每一个事件组内部的事件数量。**

不要重做游戏架构。

首先扫描整个项目，找到真实事件数据库，例如：

```text
events.json
events/*.json
data/events.json
event_groups
build_full_database.py
```

以及实际代码中使用的相关文件。

以项目真实路径为准。

---

# 六、第一步：先做现有事件盘点

在修改之前，先自动统计：

```text
事件组
当前事件数
事件ID
事件标题
开放月份
关闭月份
事件类型
体力消耗
选项数量
是否存在前后剧情关系
是否已经关联路线
```

至少覆盖项目中现有的全部事件组。

不要根据之前任何外部设计文档假设事件数量。

必须以：

> **压缩包中当前实际运行的数据**

为唯一事实源。

---

# 七、生成事件扩充前先做时间分布图

对于每一个事件组，统计：

```text
M1
M2
M3
……
M42
```

每个月当前有多少事件可以出现。

例如：

```text
AC 学业

M1   3
M2   4
M3   2
M4   1
……
M20  0
M21  0
……
```

找出：

```text
事件过少月份
完全空白月份
内容过度集中的月份
明显与大学阶段不匹配的月份
```

新增事件优先补这些位置。

不要简单：

> 每组机械增加相同数量。

---

# 八、暂时不要再以“400个事件”为硬目标

本轮不要把任务理解成：

> 必须凑满400个。

因为强行追求数量会产生大量：

```text
换名字
换课程
换NPC
换月份
但本质完全一样
```

的重复事件。

本轮的目标应该是：

> 让每个事件组在大学四年的合理阶段拥有足够丰富、持续可选、不明显重复的事件。

先根据项目当前数量做扩充。

如果用户没有明确给出最终数量：

1. 先统计当前事件数；
2. 找出每组内容空洞；
3. 优先增加缺失内容；
4. 保证事件差异性；
5. 输出新增数量；
6. 不为了达到整数目标硬生成垃圾事件。

质量优先于数量。

---

# 九、不要重新设计“四节点事件链”

此前可能存在：

```text
SEED
GROWTH
CONFLICT
FEEDBACK
```

等规划概念。

这些可以作为策划分析工具，但：

> 不得强制每4个事件组成一个固定模板。

不要把整个事件库机械写成：

```text
机会
↓
发展
↓
冲突
↓
结算
```

这样会产生大量明显重复。

实际大学生活中的事件允许有：

```text
单独事件
两阶段事件
三阶段事件
长期剧情
一次性事件
结果反馈事件
周期性机会
```

结构应根据剧情需要决定。

---

# 十、前后事件关系怎么处理

这是新增事件时非常重要的一项。

每生成一个事件，必须先判断：

```text
它是否依赖另一个事件？
```

分成两类。

## A. 独立事件

例如：

```text
图书馆抢座
朋友临时约饭
社团宣传活动
选修课临时作业
宿舍停电
校园招聘宣讲
```

它们可以单独出现。

不需要人为制造：

```text
前置事件
后续事件
```

## B. 有明显前后顺序的事件

例如：

### 求职

```text
准备简历
↓
第一次投递
↓
收到面试
↓
参加面试
↓
收到Offer / 拒信
```

显然存在前后顺序。

### 考研

```text
决定考研
↓
择校
↓
开始复习
↓
报名
↓
初试
↓
出分
↓
复试 / 调剂
```

### 保研

```text
关注排名
↓
科研/竞赛积累
↓
准备材料
↓
夏令营
↓
预推免
↓
最终确认
```

### 恋爱

```text
认识
↓
熟悉
↓
关系升级
↓
产生矛盾
↓
修复 / 分手
```

### AI-Lab

```text
接触线索
↓
第一次任务
↓
评审
↓
返工
↓
核心任务
↓
进入核心团队
```

这些事件必须保证：

> 后面的剧情在大学时间上不会出现在前面的剧情之前。

---

# 十一、优先通过“月份安排”保证顺序

因为当前游戏的主要事件可见机制是：

```text
开放月份
```

所以新增事件的前后关系：

> 优先通过合理安排时间窗口解决。

例如：

```text
WK_新简历
M18~M22

WK_第一次投递
M20~M24

WK_第一次面试
M22~M27

WK_暑期实习
M26~M31

WK_秋招
M33~M38
```

这样即使不重构事件系统：

> 故事仍然自然符合大学阶段。

---

# 十二、不要擅自增加新的复杂前置条件系统

如果项目当前已经存在：

```text
required_flags
prerequisite
previous_event
route_state
```

等机制：

可以继续使用。

但只能：

> 沿用现有实现。

禁止本轮新建：

```text
复杂事件图
DAG任务系统
事件队列
future_event scheduler
动态剧情导演系统
```

这些都不是本任务目标。

---

# 十三、新事件必须符合大学时间阶段

如果项目当前时间轴为：

```text
M1~M42
```

则新增事件需要遵循大学生命周期。

实际月份必须结合项目现有定义再次确认，不要机械照抄下面月份。

可以大致参考：

## 大一前期

```text
新生报到
宿舍磨合
第一次选课
社团招新
班级认识
第一次考试
校园探索
```

## 大一下 / 大二

```text
专业学习逐渐深入
社团骨干
第一次竞赛
第一次大型课程项目
朋友关系稳定
恋爱机会增加
科研/职业方向开始出现
```

## 大二下 / 大三

```text
实习
科研
竞赛
专业项目
保研准备
考研决策
职业探索
AI-Lab深入
复杂人际关系
```

## 大三下 / 大四前期

```text
考研冲刺
推免
暑期实习
秋招
Offer
毕业设计
城市选择
关系去向
```

## 大四后期

```text
毕业设计
答辩
签约
复试
毕业手续
宿舍告别
关系收口
家庭讨论
毕业去向
```

---

# 十四、必须检查时间冲突

严禁出现明显不合理情况，例如：

```text
M3 秋招
M5 毕业答辩
大一直接参加研究生复试
大一收到正式校招Offer
大二开始毕业离校
考研初试之前已经发生调剂
确认恋爱之前发生周年纪念
第一次实习之前出现转正答辩
第一次接触AI-Lab之前已经成为核心成员
```

每个新增事件都需要通过：

```text
TIMELINE_CHECK
```

---

# 十五、文案必须符合对应大学阶段

除了月份合理：

> 人物说话方式和事件背景也必须合理。

例如大一：

可以写：

```text
辅导员
室友
班长
社团招新
选课
第一次考试
```

不应该大量写：

```text
HR谈薪
硕导选择
毕业设计答辩
正式转正
```

大四则反过来。

---

# 十六、必须主动控制“重复事件”

这是本轮最重要的内容质量要求之一。

不能只比较标题。

必须检查至少四层重复：

## 1. 文案重复

例如：

```text
“室友叫你出去吃饭”
“同学邀请你聚餐”
“朋友约你晚上吃饭”
```

如果剧情和选择完全相同：

属于重复。

## 2. 冲突结构重复

例如连续大量事件都是：

```text
学习 vs 社交
```

即使文案完全不同：

也属于机制重复。

## 3. 选项重复

禁止大量出现：

```text
A 全力投入
B 适度投入
C 放弃
```

或者：

```text
A 去
B 不去
```

反复出现。

## 4. 数值重复

如果大量事件都是：

```text
TU -2
能力 +3
压力 +1
```

只换文字：

也属于低质量重复。

---

# 十七、新事件要增加“大学生活差异”，而不是增加换皮

新增内容优先覆盖：

```text
不同地点
不同人物关系
不同成长阶段
不同资源冲突
不同后果类型
不同选择方式
```

例如“社交”不应该全部是：

```text
有人约你参加活动
```

还可以有：

```text
室友
班级
朋友
社团
学长
老师
合作项目
误会
冷场
求助
合作
关系维护
拒绝
领导责任
信息交换
告别
```

---

# 十八、各事件组应保持自己的主题边界

不要让所有事件组变成相同的生活事件。

例如：

## 学业

核心是：

```text
课程
考试
项目
学习方法
老师
专业能力
毕业设计
```

## 社交

核心是：

```text
室友
同学
朋友
社团
团队
关系
社会连接
```

## 恋爱

核心是：

```text
相识
关系升级
时间安排
沟通
矛盾
支持
未来
```

## 家庭

核心是：

```text
父母
家庭期待
生活费
返乡
独立
家庭责任
```

## 健康

核心是：

```text
睡眠
运动
饮食
疲劳
压力
恢复
```

## 工作

核心是：

```text
职业认知
简历
投递
面试
实习
职场
秋招
Offer
```

## 考研

核心是：

```text
选择
择校
复习
报名
模考
初试
复试
调剂
```

## 保研

核心是：

```text
GPA
排名
科研
竞赛
导师
材料
夏令营
预推免
```

## AI-Lab

核心是：

```text
技术探索
复现
任务
评审
返工
工程
研究
团队
核心成员
```

公共生活等其他组：

以项目当前定义为准。

---

# 十九、不要擅自增加大量新NPC

优先复用已有：

```text
室友
班长
同学
学长/学姐
辅导员
专业老师
父母
伴侣
项目队友
实习导师
AI-Lab成员
```

只有确实需要新的社会关系时再增加人物。

不要每个事件创造一个只出现一次的新名字。

---

# 二十、保持现有事件格式

新事件必须：

> 完全按照项目当前实际 event schema 编写。

不要因为外部文档里出现过：

```text
chain_id
future_event
story_role
```

就自动往项目里加。

必须先读取真实 schema。

现有字段是什么：

就使用什么。

例如如果实际项目是：

```json
{
  "id": "...",
  "group": "...",
  "title": "...",
  "open_month": 0,
  "close_month": 0,
  "priority": "...",
  "options": []
}
```

那么：

> 按实际结构生成。

禁止擅自升级 schema。

---

# 二十一、保持现有数值系统

不得重新设计：

```text
TU
EP
学业
健康
社交
家庭
关系
能力
路线状态
```

等数值。

必须读取项目中已有数值定义。

新增事件：

> 只能使用当前已经存在的合法 effect key。

禁止模型自己发明：

```text
confidence
career_score
romance_power
AI_skill_2
```

等不存在字段。

---

# 二十二、体力成本必须有差异

不要把所有事件都写成同样成本。

根据现实投入区分：

### 很轻

例如：

```text
回复消息
听一次宣讲
短暂聊天
```

### 普通

例如：

```text
参加活动
一次约会
一次课程复习
```

### 较高

例如：

```text
比赛
大型项目
高强度复习
连续赶工
```

具体数值：

> 必须使用项目现有 TU / EP 标尺。

不要自行重新规定范围。

---

# 二十三、选择必须是现实行动

选项文字应该写：

```text
去图书馆把两门最危险的课先过一遍
```

而不是：

```text
努力学习
```

应该写：

```text
给室友发消息，把问题约到晚上当面谈
```

而不是：

```text
积极沟通
```

应该写：

```text
接受面试，把周五下午的实验课提前和老师说明
```

而不是：

```text
抓住机会
```

---

# 二十四、不要求每个事件固定三选一

根据事情本身：

```text
2个选项
3个选项
4个选项
```

都可以。

不要为了格式统一：

> 强制所有事件三选一。

---

# 二十五、新事件不能存在绝对最优项

例如：

```text
A
体力最少
收益最高
没有风险
没有关系代价
```

这种选择无意义。

不同选择应该体现：

```text
时间
精力
成绩
关系
健康
机会
风险
```

中的取舍。

---

# 二十六、不要把游戏改成问卷

禁止连续出现：

```text
你更看重什么？
A 学习
B 社交
C 健康
```

事件首先应该是：

> 发生了一件具体事情。

然后玩家：

> 决定怎么办。

---

# 二十七、修改策略：先规划，再生成

不要打开项目以后直接一次生成几百个事件。

执行顺序：

## Round A：盘点

生成：

```text
event_inventory_before.md
```

记录当前事件库。

## Round B：缺口分析

生成：

```text
event_expansion_plan.md
```

每组说明：

```text
当前事件数
时间空洞
主题空洞
需要新增的事件类型
计划新增多少
```

## Round C：新增事件

按照事件组分批修改。

推荐：

```text
一次只处理一个事件组
```

不要十组同时生成。

## Round D：全库审计

完成后再：

```text
重复检查
时间检查
ID检查
JSON检查
运行检查
```

---

# 二十八、已有事件原则上不重写

本轮主要任务是：

> 增加事件。

不要趁机大规模修改现有事件。

只有发现以下问题才允许小范围修复：

```text
明显月份错误
无法解析
重复ID
引用不存在字段
明显前后顺序错误
明显文案矛盾
```

而且必须：

> 在报告中单独记录。

---

# 二十九、ID规则

必须读取项目当前 ID 规则。

例如现有：

```text
AC_001
AC_002
WK_001
```

则新事件继续：

```text
AC_xxx
WK_xxx
```

不得：

```text
重排全部编号
重新给旧事件编号
改变已有结局引用的ID
```

旧 ID 必须稳定。

---

# 三十、不得破坏结局系统

如果当前：

```text
某些事件ID
某些Flag
某些路线状态
```

已经被结局判断使用：

禁止修改其语义。

新增事件可以补充路线体验：

但不得：

```text
重新设计结局
修改结局编号
改变结局触发逻辑
```

除非这是另一个明确任务。

---

# 三十一、不得改变开放分支条件

工作、考研、保研、AI-Lab 等分支如何开放：

> 完全沿用现有代码。

本轮不调整：

```text
开启阈值
开启月份
路线冲突
路线关闭规则
隐藏路线发现方式
```

只增加：

> 分支开放后可玩的内容。

---

# 三十二、重要：分支开放后不要被“事件不足”卡死

新增内容的目标之一就是解决：

```text
分支已经开放
但某些月份几乎没有事情可做
```

所以新增事件应该让：

> 已开放分支在合理大学阶段内大部分月份都有可选内容。

但不需要保证：

```text
每一个月
每一个分支
必须有全新事件
```

允许已有事件拥有合理开放窗口。

---

# 三十三、自动重复检查

建立：

```text
event_duplicate_audit.md
```

至少从以下维度检查：

```text
标题相似
背景相似
核心冲突相似
选项结构相似
效果结构相似
```

对于疑似重复：

标记：

```text
KEEP
REWRITE
MERGE
DELETE_NEW_EVENT
```

优先删除新生成的低质量重复事件。

不要为了数量留下垃圾。

---

# 三十四、时间审计

生成：

```text
event_timeline_audit.md
```

至少检查：

```text
事件月份
大学阶段
剧情前后关系
路线节点顺序
毕业相关事件
考试相关事件
实习/秋招
考研/保研
恋爱长期关系
AI-Lab成长
```

输出所有：

```text
TIMELINE_CONFLICT
SEQUENCE_CONFLICT
```

并修复。

---

# 三十五、运行验证

修改完成后：

1. 验证所有 JSON / 数据文件可以解析；
2. 运行项目已有测试；
3. 运行项目构建；
4. 启动游戏；
5. 至少人工/自动模拟多个连续月份；
6. 检查事件是否按照月份显示；
7. 检查体力不足时事件的现有表现是否正常；
8. 检查分支没有因为本轮修改而随机消失；
9. 检查旧事件仍可正常触发；
10. 检查结局和路线引用未损坏。

---

# 三十六、必须做回归测试

至少验证：

```text
同一个已经开放的分支
连续多个月仍然可以选择
```

例如：

```text
M10
工作路线已开放
→ 可以进入

M11
→ 仍可以进入

M12
→ 仍可以进入
```

不能因为：

```text
某个事件被完成
```

就错误关闭整个分支。

---

# 三十七、必须验证“时间可见 + 体力可选”

选取至少 10 个新增事件测试：

### Case A

```text
当前月份 < open_month
```

结果：

```text
不可见
```

### Case B

```text
open_month <= current_month <= close_month
+
体力足够
```

结果：

```text
可见
可选
```

### Case C

```text
事件月份满足
+
体力不足
```

结果：

```text
按照原项目设计：
仍然可见
但不能执行
```

不得改变为直接隐藏。

---

# 三十八、最终交付报告

生成：

```text
event_expansion_report.md
```

至少包括：

## 1. 修改前统计

```text
每组事件数
总事件数
```

## 2. 修改后统计

```text
每组事件数
总事件数
新增事件数
```

## 3. 时间覆盖

```text
M1~M42
每个事件组覆盖情况
```

## 4. 前后顺序检查

```text
存在顺序关系的事件
如何保证顺序合理
```

## 5. 重复检查

```text
发现多少疑似重复
删除/重写多少
```

## 6. 原有玩法保护

明确确认：

```text
月度循环未修改
分支开放逻辑未修改
分支选择逻辑未修改
体力机制未修改
结局逻辑未修改
事件 schema 未重构
```

## 7. 文件变化

列出：

```text
修改文件
新增文件
没有修改的核心文件
```

---

# 三十九、最终成功标准

只有满足以下全部要求，本任务才算成功：

1. 游戏原有月度循环不变；
2. 已开放分支每个月仍然可以选择；
3. 没有新增随机分支抽取系统；
4. 没有新增复杂事件导演器；
5. 事件主要按开放月份决定是否可见；
6. 体力主要决定事件是否能执行；
7. 原有体力系统不变；
8. 原有路线开放逻辑不变；
9. 原有结局逻辑不变；
10. 只增加和优化事件内容；
11. 新事件符合真实大学时间阶段；
12. 有前后关系的事件顺序正确；
13. 独立事件不强行制造事件链；
14. 没有机械四节点模板；
15. 没有为了达到400而制造大量换皮事件；
16. 新事件不存在明显重复；
17. 选项不是大量重复“努力/平衡/放弃”；
18. 新事件沿用真实 event schema；
19. 没有发明新数值字段；
20. 没有重新编号旧事件；
21. 旧事件仍能正常运行；
22. 项目可以正常构建和试玩；
23. 输出完整扩充报告和重复/时间审计。

---

# 四十、任务的最终设计原则

本轮最重要的判断不是：

> “事件数量够不够多？”

而是：

> “当一个分支已经开放以后，玩家在大学四年的合理月份里，是否持续有足够丰富、符合阶段、不会明显重复的事情可以选择？”

不要重做玩法。

不要设计新系统。

不要为了数量增加复杂度。

保持当前：

```text
开放分支
→ 每月可进入
→ 看当前月份事件
→ 有体力就做
→ 结算
→ 继续选择
```

这一玩法完整不变。

本轮只把：

> **每个分支里面的内容做厚、做丰富、做合理。**

严格按照这一原则修改项目。
